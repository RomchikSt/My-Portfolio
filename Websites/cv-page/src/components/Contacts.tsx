import styled from "styled-components";

const StyledContacts = styled.div`
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;

function Contacts() {
  return <StyledContacts>Contacts</StyledContacts>;
}

export default Contacts;
